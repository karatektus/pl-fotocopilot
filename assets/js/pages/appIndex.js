var appIndex = function () {
    "use strict";

    return {
        /**
         * Init
         */
        init: function () {
            window.fitText( document.getElementsByClassName("fittext"), 0.53 );
        }
    };
}();